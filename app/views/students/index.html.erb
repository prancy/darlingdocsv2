<h1>All Students</h1>

<ul>
    <% @students.each do |student| %>
        <li>
            <strong><%= student.name %></strong> - <%= student.age %> <%= student.gender %>
        </li>
    <% end %>
</ul>