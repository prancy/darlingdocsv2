<h1>All Students</h1>

<ul>
    <% @students.each do |student| %>
        <li>
            <strong><%= link_to student.name, student_path(student) %></strong> - <%= student.age %> <%= student.gender %> 
        </li>
    <% end %>
</ul>