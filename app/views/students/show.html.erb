<h1>This is <%= @student.name %>'s Progress </h1>

<%= link_to "See All Students", students_path %>

<%= link_to "Add a Report", new_student_report_path(@student.id) %>

<% @student.reports.each do |report| %>

    <%= report.participation %>
    <%= report.behavior %>
    <%= report.homework %>

<% end %>